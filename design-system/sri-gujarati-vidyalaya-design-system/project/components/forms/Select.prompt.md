Labelled dropdown with custom chevron, focus ring and error states.

```jsx
<Select label="Applying for" required placeholder="Select a grade"
  options={["Pre-KG", "Class I", "Class XI — Science", "Class XI — Commerce"]} />
```

Options accept plain strings or `{ value, label }` objects.
