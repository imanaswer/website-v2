Custom checkbox with maroon fill and an optional description line.

```jsx
<Checkbox label="I agree to be contacted about admissions"
  description="We'll only use your details to respond to this enquiry." />
```

Works controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
